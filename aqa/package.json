{
  "scripts": {
    "test": "mocha specs/pow.spec.js --reporter mochawesome",
    "hometask": "mocha specs/camelize.spec.js --reporter mochawesome"
  },
  "devDependencies": {
    "chai": "^4.3.4",
    "mocha": "^9.0.2",
    "mochawesome": "^6.2.2"
  }
}
